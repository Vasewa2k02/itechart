import { Layout } from 'antd';
import styled from 'styled-components';

import { COMMON_STYLES } from 'enums/common-styles.enum';

export const Container = styled(Layout)({ padding: COMMON_STYLES.indentLarge });
